// import { ISPSuperField } from '../../models';

export interface IFieldConfigItemReactState {
  editmode: boolean;
  id: string;
  visible: boolean;
  title: string;
  type: string;
  name: string;
  required: boolean;
}
